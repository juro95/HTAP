1)git clone the repository in the glcoud via command line

2)cd into the cloned repository

3) enter the following command:

gcloud app deploy --quiet app.yaml --project (YOUR PROJECT ID) --promote

4) get browser link with the following command:

glcoud app browse